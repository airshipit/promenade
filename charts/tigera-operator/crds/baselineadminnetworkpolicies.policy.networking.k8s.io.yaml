apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    api-approved.kubernetes.io: "https://github.com/kubernetes-sigs/network-policy-api/pull/30"
  name: baselineadminnetworkpolicies.policy.networking.k8s.io
spec:
  group: policy.networking.k8s.io
  names:
    kind: BaselineAdminNetworkPolicy
    listKind: BaselineAdminNetworkPolicyList
    plural: baselineadminnetworkpolicies
    shortNames:
      - banp
    singular: baselineadminnetworkpolicy
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          description: BaselineAdminNetworkPolicy is a cluster-scoped resource that
            allows administrators to define baseline network policies that can be
            overridden by AdminNetworkPolicy resources.
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              description: Specification of the desired behavior of BaselineAdminNetworkPolicy.
              type: object
              required:
                - subject
              properties:
                subject:
                  description: Subject defines the pods to which this BaselineAdminNetworkPolicy
                    applies.
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                ingress:
                  description: Ingress is the list of ingress rules to apply to the
                    subject.
                  type: array
                  items:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                egress:
                  description: Egress is the list of egress rules to apply to the
                    subject.
                  type: array
                  items:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
